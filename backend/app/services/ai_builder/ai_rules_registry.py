AI_RULES_REGISTRY = {
    "Agent": "Requires an LLM connected to 'llm'. Connect BufferMemory to 'memory' for normal chat/agent flows. Connect tool outputs to 'tools'. Use user_prompt_template='${{input}}' for the user's canvas chat input.",
    "BufferMemory": "Memory provider. Connect its 'memory' output to an Agent's 'memory' input. Do not place it in the main left-to-right processing chain.",
    "VectorStoreOrchestrator": "Requires 'embedder' input from OpenAIEmbeddingsProvider. MUST receive 'documents' input from a WebScraper, DocumentLoader or ChunkSplitter.",
    "WebhookTrigger": "Used to start API/Event workflows. DO NOT use EndNode in a webhook flow; ALWAYS end it with a RespondToWebhook node instead.",
    "RespondToWebhook": "MUST be the final node in a Webhook workflow. Connect its input to a processing node (Agent, CodeNode, ConditionNode). Its response_body should use ${{upstream_node_name}} to reference the data from the connected upstream node.",
    "CohereRerankerProvider": "Optional side-provider. Connect its 'reranker' output to RetrieverProvider's 'reranker' input. Only use when user explicitly asks for improved retrieval quality.",
    "RetrieverProvider": "Requires 'embedder' input from OpenAIEmbeddingsProvider. Connect its 'retriever_tool' output to an Agent's 'tools' input. Use ONLY when the user wants to QUERY an existing vector store (not for storing new documents).",
    "TavilySearch": "Use this for LIVE internet/web search. Connect its 'search_tool' output to an Agent's 'tools' input. Do NOT use this for processing user's own documents. IF data is coming from a previous node (e.g. WebhookTrigger or user chat), you MUST set the 'query' property of this node to reference it (e.g., query: ${{upstream_node_name}}).",
    "HttpRequest": "Use this when the user needs to call a specific REST API endpoint. This is NOT for web search (use TavilySearch) and NOT for scraping web pages (use WebScraper). Connect 'response', 'content', or 'documents' downstream based on intent.",
    "MarkItDownTool": "Tool provider for converting MinIO/S3 documents to Markdown. Connect its 'tool' output to an Agent's 'tools' input.",
    "ConditionNode": "value1 is the SOURCE TEXT to test (set it to ${{upstream_node_name}} to reference the incoming data). value2 is the KEYWORD to search for. Operation 'contains' checks if value1 contains value2. For MULTIPLE keywords (OR logic), use operation='regex' and set value2 to a regex pattern like 'keyword1|keyword2'. Do NOT place an Agent before this node just for text matching; connect the data source directly.",
    "CodeNode": "OUTPUT MECHANISM (CRITICAL): The CodeNode captures output EXCLUSIVELY via standard output (STDOUT). You MUST use `print(your_data)` in Python or `console.log(your_data)` in JavaScript. To read incoming data, use the exact incoming node's snake_case name inside the template WITHOUT QUOTES! Example: `data = ${{webhook_trigger}}` (NEVER `data = '${{webhook_trigger}}'`).",
    "JsonParserNode": "Use this to parse LLM/API/webhook text into JSON or YAML. Connect upstream data to 'input' and use 'output' downstream.",
    "CryptographyNode": "Use only when the workflow explicitly needs encrypt/decrypt/sign/verify/generate key behavior. Connect upstream data to 'input_data' and configure action/crypto_type/cipher.",
    "ChunkSplitter": "Receives 'documents' from WebScraper or DocumentLoader. If the goal is to store in a database (RAG), connect its 'chunks' output to VectorStoreOrchestrator's 'documents' input. If the goal is just processing or summarizing, connect 'chunks' to an Agent or CodeNode.",
    "OpenAIChat": "This is an LLM Provider. Connect its 'llm' output to an Agent's 'llm' input. Do NOT connect it directly in the main processing chain; it is a side-provider like Embeddings.",
    "OpenAICompatibleNode": "Use this LLM provider for OpenRouter, Groq, DeepSeek, Ollama, LocalAI, vLLM, or any custom OpenAI-compatible endpoint. Connect 'llm' to an Agent or red-team node LLM input.",
    "OpenAIEmbeddingsProvider": "This is a side-provider. Connect its 'embedder' output to VectorStoreOrchestrator's or RetrieverProvider's 'embedder' input. Do NOT place it in the main processing chain.",
    "WebScraper": "Use this to SCRAPE specific web page URLs and produce Documents. If the user wants to store/vectorize the data, connect its 'documents' output to a ChunkSplitter. If the user just wants to summarize or read the page, you can connect it directly to an Agent's input. This is NOT for general web search (use TavilySearch for that).",
    "DocumentLoader": "Use this to load user's OWN documents. If storing in a vector DB, connect to ChunkSplitter. If just summarizing, connect directly to an Agent. This is NOT for web search or scraping.",
    "StartNode": "This is only a trigger/entrypoint. It passes the user's initial_input text (which might be empty) to its output. Do NOT reference it with ${{Start}} in downstream node prompts. The downstream node receives data automatically via edge connections, not template variables.",
    "KafkaConsumer": "Use this as the start of a flow when the user wants to listen to or consume messages from an Apache Kafka topic. Connect its 'kafka_data' output to a processing node. Do NOT mix this with WebhookTrigger or StartNode.",
    "KafkaProducer": "Use this to send/publish messages to an Apache Kafka topic. Connect upstream data to its 'input' handle, then connect its 'output' to EndNode.",
    "TimerStart": "Use this to trigger a workflow on a schedule (Cron/Timer) automatically. Do NOT use downstream of other nodes; it is a starting trigger.",
    "StringInputNode": "A simple processor node that statically defines a string value and passes it downstream. Usually not needed if data can be injected via prompt fields.",
    "ErrorTrigger": "Use only for an error-handling workflow triggered by another failed workflow. Connect 'error_data' to CodeNode, Agent, HttpRequest, or RespondToWebhook-style processors.",
    "LLMRedTeam": "Security test processor. Requires input plus simulator_llm and evaluator_llm connections from LLM provider nodes.",
    "CustomRedTeam": "Security test processor using a custom prompt dataset. Requires input and evaluator_llm.",
    "AgenticRedTeam": "Agentic security test processor. Requires input plus simulator_llm and evaluator_llm connections from LLM provider nodes."
}
